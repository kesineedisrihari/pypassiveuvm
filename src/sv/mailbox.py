class mailbox:

    def __init__(self, bound : int = 0, T = None):
        self.m = []
        self.bound = bound

    def put(t):
        if self.bound > 0:
            if self.bound == self.size:
                sys.exit()

            self.m.append(t)

    def peek(t):
        return self.m.pop(t)

    def get(t):
        sys.exit('event scheduler need to be implemented')

    def num():
        return len(slef.m)

